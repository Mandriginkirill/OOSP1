package Factory.Vehicles;

import Factory.Vehicle;

public class Car extends Vehicle {
    public Car(boolean up, boolean down, boolean left, boolean right) {
        super(up, down, left, right);
    }
}
